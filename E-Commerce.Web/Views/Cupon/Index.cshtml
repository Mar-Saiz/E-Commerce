@model IEnumerable<CuponViewModel>
@{
    ViewData["Title"] = "Cupones index";
}

<div class="container mt-4">
    <div class="row mb-2">
        <div class="col text-end">
            <a asp-action="Crear" asp-controller="Cupon" class="btn btn-success btn-lg">
                <i class="fa fa-plus me-2"></i>Agregar nuevo Cupon
            </a>
        </div>
    </div>
    <div class="card shadow rounded">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">Lista de cupones</h4>
        </div>
        <div class="table">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-hover table-bordered align-middle text-center" id="tblCountry">
                        <thead class="table-dark">
                            <tr>
                                <th>#</th>
                                <th>Codigo</th>
                                <th>Descuento</th>
                                <th>FechaExpiracion</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(m => item.Id)</td>
                                    <td>@Html.DisplayFor(m => item.Codigo)</td>
                                    <td>@Html.DisplayFor(m => item.Descuento)</td>
                                    <td>@Html.DisplayFor(m => item.FechaExpiracion)</td>
                                    <td>
                                        <a class="btn btn-sm btn-warning me-1" asp-action="Editar" asp-route-id="@item.Id">
                                            <i class="fa fa-pencil"></i> Editar
                                        </a>
                                        <a class="btn btn-sm btn-info text-white me-1" asp-action="Detalles" asp-route-id="@item.Id">
                                            <i class="fa fa-info-circle"></i> Detalles
                                        </a>
                                        <a class="btn btn-sm btn-danger" asp-action="Borrar" asp-route-id="@item.Id">
                                            <i class="fa fa-trash"></i> Borrar
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-warning text-center" role="alert">
                    No hay registros disponibles.
                </div>
            }
        </div>
    </div>
</div>